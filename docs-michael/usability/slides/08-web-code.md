# Slide 8 — Web Code

> Self-contained spec for building this slide. You should not need any other file to produce it.

## The one line (put this big on the slide)

**Web Code** — A Web User whose work is mostly code in code mode (`agent.code`). Inherits [Consumer](../../consumer/consumer.md) and [Web User](../web-user/web-user.md).

## Who this person is

A Web User whose work is mostly code in code mode (`agent.code`). Inherits [Consumer](../../consumer/consumer.md) and [Web User](../web-user/web-user.md).

## What concerns them (the slide body)

This persona's own concerns — show them as a set of cards/tiles, one per group:

**Explanation** (2):

- [Agent reasoning is hidden in a separate panel
- [Tool calls show what ran but not why

**Safety & Undo** (3):

- [File changes are applied with no preview or approval
- [Destructive external actions fire without confirmation
- [Sent messages and API calls can't be undone

**Progress** (1):

- [No step-wise progress or ETA while a skill runs

**Choosing** (1):

- [Agent modes have no user-facing names or descriptions

**Keyboard** (2):

- [The UI can't be driven fully by keyboard
- [Core actions have no keyboard shortcuts

## Speaker notes (short)

"Web Code: A Web User whose work is mostly code in code mode (`agent.code`). Inherits [Consumer](../../consumer/consumer.md) and [Web User](../web-user/web-user.md). These are the 9 concerns specific to them; anything shared comes from the base persona above them."
