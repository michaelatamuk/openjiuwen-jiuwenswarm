# No single answer to what a gateway can reach

*Concern: Connection & Security*

Authority is spread across permissions.tools, rules, file_guard, external_directory, owner_scopes and approval_overrides with per-channel differences, so a builder can't state an app's blast radius in one sentence.

_Source: OpenJiuwen action board (by domain)._ 
