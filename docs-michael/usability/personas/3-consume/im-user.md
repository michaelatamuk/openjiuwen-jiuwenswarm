# IM User

*A Consumer who only talks to a running instance through a messaging (IM) bot — no Web UI.*

Shares the [Consumer](consumer.md) base; IM has no concerns that are unique to it, so it owns no findings of its own. All the findings that apply come from the Consumer base.
