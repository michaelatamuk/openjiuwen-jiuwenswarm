# IM User

*A Consume User who only talks to a running instance through a messaging (IM) bot — no Web UI.*

Shares the [Consume User](../consume-user.md) base; IM has no concerns that are unique to it, so it owns no findings of its own. All the findings that apply come from the Consume User base.
