# Default permissions are off, contradicting the README

*Concern: Running & Managing the Instance*

All shipped config templates set permissions.enabled:false, so a user who reads the README believes tools are gated when they aren't; tiered policy and Smart Approval are dark on a fresh install.

_Source: OpenJiuwen action board (by domain)._ 
