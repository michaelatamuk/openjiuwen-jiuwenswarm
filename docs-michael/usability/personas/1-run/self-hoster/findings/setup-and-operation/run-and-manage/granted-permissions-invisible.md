# Granted permissions are invisible once granted

*Concern: Running & Managing the Instance*

Always-allow choices write into approval_overrides with no in-UI view of what has accumulated, so a user can't see or revoke what they've allowed.

_Source: OpenJiuwen action board (by domain)._ 
