# Docs are invisible to agents evaluating us (no llms.txt)

*Concern: Documentation & Stability*

There's no llms.txt/llms-full.txt index, so an agent pointed at the docs only gets whatever the crawler happens to find instead of a curated, self-updating summary.

_Source: OpenJiuwen action board (by domain)._ 
